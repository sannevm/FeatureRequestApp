import { FeatureRequestItem } from './feature-request-item';

describe('FeatureRequestItem', () => {
  it('should create an instance', () => {
    expect(new FeatureRequestItem()).toBeTruthy();
  });
});
